import 'package:flutter/material.dart';

import 'components/body.dart';

class VoteTypeScreen extends StatelessWidget {
  static String routeName = '/vote_type_screen';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
